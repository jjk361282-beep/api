# this project is todoapp api
## used stack for:

- apiflask
- sqlalchemy
- mashmolw

## structure the project 
 the project a structor form mvc 
 ```
 ```
## run app:

```bash
$ uv sync 

$ uv run flask db upgrade

```
## or

```bash

$ python -m venv .venv

$ pip install -r requirement.txt 

$ flask db upgrade

$ flask run 

```
